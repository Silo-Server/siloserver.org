---
interface Props {
  class?: string;
}

const { class: extra = "" } = Astro.props;
const baseUrl = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<span class={`brand-lockup ${extra}`} role="img" aria-label="Silo">
  <img
    class="brand-lockup__on-light"
    src={`${baseUrl}/brand/silo-wordmark-dark.png`}
    alt=""
    aria-hidden="true"
    width="191"
    height="100"
  />
  <img
    class="brand-lockup__on-dark"
    src={`${baseUrl}/brand/silo-wordmark-white.png`}
    alt=""
    aria-hidden="true"
    width="191"
    height="100"
  />
</span>
